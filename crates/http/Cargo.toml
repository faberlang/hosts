[package]
name = "http"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[lints.clippy]
pedantic = "deny"

[dependencies]
faber = { package = "faber-runtime", path = "../../../faber-runtime" }
host-kernel = { path = "../host-kernel" }
ureq = "2"
