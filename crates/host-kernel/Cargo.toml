[package]
name = "host-kernel"
version = "0.1.0"
edition = "2021"
description = "Transport-neutral Faber host provider kernel"
license = "MIT"
readme = "README.md"

[lints.clippy]
pedantic = "deny"

[dependencies]
faber = { path = "../../../faber/runtime/rust" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
