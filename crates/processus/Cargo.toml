[package]
name = "processus"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lints.clippy]
pedantic = "deny"

[dependencies]
faber = { path = "../../../faber/runtime/rust" }
host-kernel = { path = "../host-kernel" }
libc = "0.2"
