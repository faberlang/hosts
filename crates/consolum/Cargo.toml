[package]
name = "consolum"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lints.clippy]
pedantic = "deny"

[dependencies]
faber = { path = "../../../faber/runtime/rust" }
host-kernel = { path = "../host-kernel" }
libc = "0.2"

[dev-dependencies]
hygiene-ratchet = { path = "../../../radix/crates/hygiene-ratchet" }
