[package]
name = "host-native"
version = "0.1.0"
edition = "2021"
description = "Bounded native Faber host dispatch adapter"
license = "MIT"
readme = "README.md"

[lints.clippy]
pedantic = "deny"

[dependencies]
faber = { path = "../../../faber/runtime/rust" }
host-kernel = { path = "../host-kernel" }

[dev-dependencies]
hygiene-ratchet = { path = "../../../radix/crates/hygiene-ratchet" }
faber = { path = "../../../faber/runtime/rust", features = ["test-support"] }
